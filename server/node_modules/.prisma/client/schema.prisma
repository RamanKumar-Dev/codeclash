// This is Prisma's schema file, generated from the existing SQL schema
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  username     String   @unique
  email        String   @unique
  passwordHash String   @map("password_hash")
  elo          Int      @default(1000)
  wins         Int      @default(0)
  losses       Int      @default(0)
  hp           Int      @default(500) // Changed from 100 to 500
  maxHp        Int      @default(500) // Changed from 100 to 500
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  // Relations
  matchesAsPlayer1 Match[]        @relation("Player1Matches")
  matchesAsPlayer2 Match[]        @relation("Player2Matches")
  matchesWon       Match[]        @relation("WinnerMatches")
  submissions      Submission[]
  battleSpells     BattleSpell[]
  battleDamage     BattleDamage[]

  @@map("users")
}

model Problem {
  id            String   @id @default(cuid())
  title         String
  description   String
  difficulty    String // 'easy' | 'medium' | 'hard'
  testCases     Json     @default("[]") @map("test_cases") // [{ input, expectedOutput, isHidden }]
  timeLimitMs   Int      @map("time_limit_ms")
  memoryLimitMb Int      @map("memory_limit_mb")
  tags          String[] // JSON array of tags
  examples      Json // JSON array of {input, output}
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")

  // Relations
  matches     Match[]
  submissions Submission[]

  @@map("problems")
}

model Match {
  id           String    @id @default(cuid())
  player1Id    String    @map("player1_id")
  player2Id    String    @map("player2_id")
  problemId    String    @map("problem_id")
  status       String    @default("waiting") // 'waiting' | 'active' | 'completed'
  winnerId     String?   @map("winner_id")
  startedAt    DateTime? @map("started_at")
  endedAt      DateTime? @map("ended_at")
  currentRound Int       @default(1) @map("current_round")
  createdAt    DateTime  @default(now()) @map("created_at")
  updatedAt    DateTime  @updatedAt @map("updated_at")

  // Relations
  player1      User           @relation("Player1Matches", fields: [player1Id], references: [id])
  player2      User           @relation("Player2Matches", fields: [player2Id], references: [id])
  winner       User?          @relation("WinnerMatches", fields: [winnerId], references: [id])
  problem      Problem        @relation(fields: [problemId], references: [id])
  submissions  Submission[]
  battleSpells BattleSpell[]
  battleDamage BattleDamage[]

  @@map("matches")
}

model Submission {
  id          String   @id @default(cuid())
  matchId     String   @map("match_id")
  userId      String   @map("user_id")
  problemId   String   @map("problem_id")
  code        String
  language    String
  passedTests Int      @map("passed_tests")
  totalTests  Int      @map("total_tests")
  execTimeMs  Int      @map("exec_time_ms")
  damageDealt Int      @map("damage_dealt")
  createdAt   DateTime @default(now()) @map("created_at")

  // Relations
  match   Match   @relation(fields: [matchId], references: [id])
  user    User    @relation(fields: [userId], references: [id])
  problem Problem @relation(fields: [problemId], references: [id])

  @@map("submissions")
}

model BattleSpell {
  id        String   @id @default(cuid())
  matchId   String   @map("match_id")
  casterId  String   @map("caster_id")
  targetId  String?  @map("target_id")
  spellType String   @map("spell_type") // 'hint' | 'time_freeze' | 'slow'
  damage    Int?
  duration  Int? // for time-based effects
  createdAt DateTime @default(now()) @map("created_at")

  // Relations
  match  Match @relation(fields: [matchId], references: [id])
  caster User  @relation(fields: [casterId], references: [id])

  @@map("battle_spells")
}

model BattleDamage {
  id         String   @id @default(cuid())
  matchId    String   @map("match_id")
  attackerId String   @map("attacker_id")
  targetId   String   @map("target_id")
  damage     Int
  damageType String   @map("damage_type") // 'puzzle' | 'spell'
  timestamp  DateTime @default(now()) @map("timestamp")

  // Relations
  match    Match @relation(fields: [matchId], references: [id])
  attacker User  @relation(fields: [attackerId], references: [id])

  @@map("battle_damage")
}
